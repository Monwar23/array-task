const site=[];
const link='123';
const name = 111;

console.log("site is an array : "+Array.isArray(site));
console.log("link is an array : "+Array.isArray(link));
console.log("name is an array : "+Array.isArray(name));
